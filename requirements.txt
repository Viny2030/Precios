# Ingeniería de Datos e Ingesta
pandas==2.2.2
requests==2.31.0

# Base de Datos
sqlalchemy==2.0.30
psycopg2-binary==2.9.9   # opcional — solo si usás PostgreSQL en lugar de SQLite

# Econometría y Cálculo Numérico
numpy==1.26.4
# scipy no se usa actualmente (la media geométrica se calcula con numpy),
# se deja disponible por si se extiende el análisis estadístico.
scipy==1.13.0

# Lectura de Excel (.xls) — necesario para seed_ponderaciones.py, que lee
# el archivo del INDEC en formato Excel 97-2003.
xlrd==2.0.1

# API REST (salida elegida: FastAPI, no Streamlit)
fastapi==0.111.0
uvicorn[standard]==0.30.1